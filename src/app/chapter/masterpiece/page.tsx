import ChapterPlaceholder from "@/components/quiz/ChapterPlaceholder";

export const metadata = { title: "BONDED Masterpiece" };

export default function MasterpiecePage() {
  return <ChapterPlaceholder tier="masterpiece" />;
}
